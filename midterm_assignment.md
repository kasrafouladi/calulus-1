# تکلیف ریاضی عمومی ۱ - سرفصل میانترم
## مجموع: ۲۰ سوال

---

## بخش اول: حد (۵ سوال)

**سوال ۱.** حد زیر را محاسبه کنید:

$$\lim_{x \to 0} \frac{\ln(1 + \sqrt{x} + \sqrt[3]{x})}{\ln(1 + \sqrt[3]{x} + \sqrt[4]{x})}$$

---

**سوال ۲.** حد زیر را بیابید:

$$\lim_{x \to +\infty} \left( \sqrt[n]{(x+a_1)(x+a_2)\cdots(x+a_n)} - x \right)$$

که در آن $a_1, a_2, \ldots, a_n$ اعداد حقیقی ثابت هستند.

---

**سوال ۳.** حد زیر را محاسبه کنید:

$$\lim_{x \to 0} \frac{1}{x^2} \left( \frac{1}{\sin x} - \frac{1}{x} \right)$$

---

**سوال ۴.** نشان دهید که اگر $\theta \in \mathbb{R}$ و $\theta \neq k\pi$ (که $k \in \mathbb{Z}$)، آنگاه:

$\sin 3\theta = 4\sin\theta \sin\left(\frac{\pi}{3}-\theta\right)\sin\left(\frac{\pi}{3}+\theta\right)$

---

**سوال ۵.** اگر $|a| = |b|$ و $a, b, \gamma \in \mathbb{C}$، ثابت کنید:

$|a+\gamma|^2 + |a-\gamma|^2 = |b+\gamma|^2 + |b-\gamma|^2$

---

## بخش دوم: پیوستگی (۳ سوال)

**سوال ۶.** فرض کنید $f: \mathbb{R} \to \mathbb{R}$ تابعی پیوسته باشد به طوری که برای هر $x \in \mathbb{R}$ داریم:

$$f(2x) = f(x) \cos x$$

ثابت کنید که $f$ تابعی ثابت (صفر) است.

---

**سوال ۷.** مقادیر $a$ و $b$ را طوری تعیین کنید که تابع زیر در همه جا پیوسته و مشتق‌پذیر باشد:

$$f(x) =
\\
ax^2 + b & \text{اگر } x \leq 2 
\\
bx^3 - a & \text{اگر } x > 2$$

---

**سوال ۸.** اگر $f$ تابعی پیوسته روی $[0, 1]$ باشد که $f(0) = f(1)$، ثابت کنید که حداقل یک نقطه $c$ در بازه $[0, \frac{1}{2}]$ وجود دارد که:

$$f(c) = f\left(c + \frac{1}{2}\right)$$

---

## بخش سوم: مشتق (۶ سوال)

**سوال ۹.** اگر $y = x^{x^x}$ باشد، مشتق $\frac{dy}{dx}$ را محاسبه کنید.

---

**سوال ۱۰.** مشتق ضمنی معادله زیر را نسبت به $x$ پیدا کنید:

$$x^3 + y^3 = 6xy$$

و نشان دهید که $\frac{dy}{dx}$ را بر حسب $x$ و $y$ بیابید.

---

**سوال ۱۱.** مشتق مرتبه $n$ ام تابع زیر را بیابید:

$$f(x) = \frac{1}{1 + x^2}$$

---

**سوال ۱۲.** اگر $x = t^2 + 1$ و $y = 2t - t^3$ باشد، مشتق مرتبه دوم $\frac{d^2y}{dx^2}$ را بر حسب $t$ محاسبه کنید.

---

**سوال ۱۳.** مشتق تابع زیر را با استفاده از مشتق لگاریتمی محاسبه کنید:

$$f(x) = \frac{(x+1)^3 \sqrt{x^2+1}}{(2x-1)^2}$$

---

**سوال ۱۴.** برای چه مقادیر $a$ و $b$، تابع زیر در همه جا مشتق‌پذیر است:

$$f(x) = \begin{cases} ax^2 + bx & \text{اگر } x \leq 1 \\ \sqrt{x} & \text{اگر } x > 1 \end{cases}$$

---

## بخش چهارم: کاربردهای مشتق (۴ سوال)

**سوال ۱۵.** تابع $f(x) = x^4 - 4x^3 + 10$ را بررسی کامل کنید و موارد زیر را بیابید:
- نقاط بحرانی
- ماکزیمم و مینیمم محلی و مطلق
- نقاط عطف
- بازه‌های صعودی و نزولی
- بازه‌های تقعر به بالا و تقعر به پایین

---

**سوال ۱۶.** معادله خط مماس و خط عمود (نرمال) بر منحنی زیر را در نقطه $(2, 1)$ بیابید:

$$x^2 + xy + y^2 = 7$$

---

**سوال ۱۷.** می‌خواهیم یک جعبه مستطیلی بدون درپوش بسازیم که حجم آن $32$ متر مکعب باشد. طول و عرض قاعده آن برابرند. ابعاد جعبه را طوری پیدا کنید که کمترین مقدار مصالح مصرف شود.

---

**سوال ۱۸.** نرخ تغییرات شعاع یک دایره $2$ سانتی‌متر بر ثانیه است. وقتی شعاع $5$ سانتی‌متر باشد، نرخ تغییرات مساحت دایره چقدر است؟

---

## بخش پنجم: مسائل (۲ سوال)

**سوال ۱۹.** اگر $0 < y < x$ و $p > 1$ باشد، با استفاده از قضیه میان‌مقدار لاگرانژ ثابت کنید:

$$py^{p-1}(x-y) \leq x^p - y^p \leq px^{p-1}(x-y)$$

---

**سوال ۲۰.** فرض کنید $f: [0,1] \to \mathbb{R}$ تابعی مشتق‌پذیر باشد که $f(0) = 0$ و برای همه $x$ در $[0,1]$ داریم:

$$|f'(x)| \leq |f(x)|$$

ثابت کنید که $f(x) = 0$ برای همه $x$ در بازه $[0,1]$.

---

## راهنمای نمره‌دهی:

- **بخش اول** (حد): هر سوال ۱ نمره → جمعاً **۵ نمره**
- **بخش دوم** (پیوستگی): هر سوال ۱.۵ نمره → جمعاً **۴.۵ نمره**
- **بخش سوم** (مشتق): هر سوال ۱ نمره → جمعاً **۶ نمره**
- **بخش چهارم** (کاربردهای مشتق): هر سوال ۱ نمره → جمعاً **۴ نمره**
- **بخش پنجم** (مسائل اثباتی): هر سوال ۰.۲۵ نمره → جمعاً **۰.۵ نمره**

### **جمع کل: ۲۰ نمره**

---

## نکات مهم برای دانشجویان:

۱. برای تمام سوالات اثباتی، استدلال کامل و دقیق الزامی است
۲. استفاده از قضایای اساسی (رول، لاگرانژ، مقدار میانی) باید صریح ذکر شود
۳. در محاسبات حدود، می‌توانید از روش‌های مختلف استفاده کنید:
   - بسط تیلور
   - قاعده لوپیتال
   - تبدیل‌های جبری
۴. در مسائل بهینه‌سازی، باید بررسی کنید که پاسخ شما واقعاً ماکزیمم یا مینیمم است
۵. دقت در شرایط اولیه و قیود مسائل ضروری است
