# تکلیف ریاضی عمومی ۱ - سطح پیشرفته
## مجموع: ۲۰ سوال

---

## بخش اول: حد و پیوستگی (۵ سوال)

**۱.** ثابت کنید:
$$\lim_{x \to 0} \frac{\ln(1 + \sqrt{x} + \sqrt[3]{x})}{\ln(1 + \sqrt[3]{x} + \sqrt[4]{x})} = \frac{4}{3}$$

**۲.** اگر $|a| = |b|$ و $a, b \in \mathbb{C}$، ثابت کنید:
$$|a+\gamma|^2 + |a-\gamma|^2 = |b+\gamma|^2 + |b-\gamma|^2$$

**۳.** هر یک از حدود زیر را محاسبه کنید:
1) $$\lim_{x \to +\infty} \left\{ \sqrt[3]{(x+a_1)(x+a_2)\cdots(x+a_n)} - x \right\}$$

2) $$\lim_{x \to 0} \frac{1}{x^2} \left(\frac{1}{\sin x} - \frac{1}{x}\right)$$

**۴.** فرض کنید $f: \mathbb{R} \to \mathbb{R}$ تابعی پیوسته باشد به طوری که برای هر $x \in \mathbb{R}$:
$$f(2x) = f(x)\cos x$$
ثابت کنید که $f$ تابع ثابت است.

**۵.** اگر $0 < y < x < p$ و $0 < x < y$، با استفاده از قضیه لاگرانژ ثابت کنید:
$$py^{p-1}(x-y) \leq x^p - y^p \leq px^{p-1}(x-y)$$

---

## بخش دوم: مشتق و کاربردها (۸ سوال)

**۶.** اگر $f$ تابعی دو بار مشتق‌پذیر باشد و برای همه $x$ داشته باشیم $f(x)f''(x) < 0$، ثابت کنید که فاصله بین هر دو ریشه متمایز $f$ بیشتر از $2\pi$ است.

**۷.** ثابت کنید که اگر $n$ عدد طبیعی باشد:
$$\int_0^{\pi/2} \sin^n x \, dx = \frac{1}{n}\int_0^{\pi/2} \sin^{n-2}x \cos^2 x \, dx$$
و با استفاده از این رابطه، مقدار $\int_0^{\pi/2} \sin^6 x \, dx$ را محاسبه کنید.

**۸.** نشان دهید که اگر $\theta \in \mathbb{R}$ و $\theta \neq k\pi$ (که $k \in \mathbb{Z}$)، آنگاه:
$$\sin 3\theta = 4\sin\theta \sin\left(\frac{\pi}{3}-\theta\right)\sin\left(\frac{\pi}{3}+\theta\right)$$

**۹.** فرض کنید $\alpha, \beta, \gamma \in \mathbb{C}$ و $|\alpha| = |\beta|$. ثابت کنید:
$$|a+\gamma|^2 + |a-\gamma|^2 = |\beta+\gamma|^2 + |\beta-\gamma|^2$$

**۱۰.** اگر $f$ تابعی مشتق‌پذیر در $[a,b]$ باشد و $f(a) = f(b) = 0$، ثابت کنید که برای هر $\lambda \in \mathbb{R}$، حداقل یک نقطه $c \in (a,b)$ وجود دارد که:
$$f'(c) = \lambda f(c)$$

**۱۱.** مشتق مرتبه $n$ام تابع $f(x) = \frac{1}{1+x^2}$ را محاسبه کنید.

**۱۲.** فرض کنید $f: \mathbb{R} \to \mathbb{R}$ یک تابع مشتق‌پذیر باشد که برای همه $x, y \in \mathbb{R}$:
$$f(x+y) = f(x) + f(y) + xy(x+y)$$
اگر $\lim_{x \to 0} \frac{f(x)}{x} = 1$، تابع $f$ را بیابید.

**۱۳.** برای چه مقادیر $a$ و $b$، تابع زیر در همه‌جا مشتق‌پذیر است:
$$f(x) = \begin{cases}
ax^2 + bx & x \leq 1 \\
\sqrt{x} & x > 1
\end{cases}$$

---

## بخش سوم: مسائل پیشرفته ترکیبی (۷ سوال)

**۱۴.** اثبات کنید:
$$\ln(1+x) = x - \frac{x^2}{2} + \frac{x^3}{3} - \cdots + (-1)^{n-1}\frac{x^n}{n} + O(x^n)$$

**۱۵.** فرض کنید $f$ تابعی سه بار مشتق‌پذیر روی $\mathbb{R}$ باشد. ثابت کنید حداقل یک نقطه $a \in \mathbb{R}$ وجود دارد که:
$$f(a) \cdot f'(a) \cdot f''(a) \cdot f'''(a) \geq 0$$

**۱۶.** اگر $p(x)$ چندجمله‌ای درجه $n$ باشد و $|p(x)| \leq 1$ برای همه $x \in [-1, 1]$، ثابت کنید:
$$|p'(0)| \leq n^2$$

**۱۷.** نشان دهید که معادله $x^5 + x - 1 = 0$ دقیقاً یک ریشه حقیقی در بازه $(0, 1)$ دارد و آن ریشه را با استفاده از روش نیوتن-رافسون تا دو رقم اعشار تقریب بزنید.

**۱۸.** تابع $f(x)$ را پیدا کنید که:
$$f(x) + 2f\left(\frac{1}{1-x}\right) = x \quad \text{برای همه } x \neq 0, 1$$

**۱۹.** اگر $f$ تابعی پیوسته روی $[0, 1]$ باشد که $f(0) = f(1)$، ثابت کنید که حداقل یک نقطه $c \in [0, \frac{1}{2}]$ وجود دارد که:
$$f(c) = f\left(c + \frac{1}{2}\right)$$

**۲۰.** فرض کنید $f: [0,1] \to \mathbb{R}$ تابعی مشتق‌پذیر باشد که $f(0) = 0$ و $|f'(x)| \leq |f(x)|$ برای همه $x \in [0,1]$. ثابت کنید که $f(x) = 0$ برای همه $x \in [0,1]$.

---

## راهنمای نمره‌دهی:
- بخش اول (حد و پیوستگی): هر سوال ۱ نمره - جمعاً ۵ نمره
- بخش دوم (مشتق و کاربردها): هر سوال ۱ نمره - جمعاً ۸ نمره
- بخش سوم (مسائل پیشرفته): هر سوال ۱ نمره - جمعاً ۷ نمره

**جمع کل: ۲۰ نمره**

---

## نکات مهم:
- برای تمام سوالات اثباتی، استدلال کامل و دقیق الزامی است
- استفاده از قضایای اساسی (رول، لاگرانژ، کوشی، مقدار میانی) باید صریح ذکر شود
- در محاسبات حدود، تکنیک‌های پیشرفته مثل بسط تیلور، قاعده لوپیتال یا تبدیل‌های جبری مجاز است
- دقت در شرایط اولیه و قیود مسائل ضروری است
